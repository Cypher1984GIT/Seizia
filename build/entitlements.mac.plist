<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
        The macOS build is ad-hoc signed (no Apple Developer certificate), and
        Electron's bundled frameworks carry Apple's own Team ID. Without
        disabling library validation the app refuses to load them and dies on
        launch. The JIT entitlements are what V8 needs under Hardened Runtime.
    -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
</dict>
</plist>
